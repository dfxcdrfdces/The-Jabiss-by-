local saul = Instance.new("Part")
saul.Parent = workspace.CurrentRooms
saul.Rotation = Vector3.new(0, 0, 0)
saul.Anchored = true
saul.Transparency = 1
local currentLoadedRoom = workspace.CurrentRooms[game:GetService("ReplicatedStorage").GameData.LatestRoom.Value]

 local num=math.floor(1)
saul.CFrame=currentLoadedRoom.Base.CFrame+Vector3.new(0,5.4,0)

local chest=game:GetObjects("rbxassetid://12087595926")[1]
chest.Parent = workspace.CurrentRooms[game:GetService("ReplicatedStorage").GameData.LatestRoom.Value]
chest:PivotTo(saul.CFrame * CFrame.new(0, 0, 0))
wait(3.5)
-- The Sounds
function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
local clock = Instance.new("Sound", workspace)
clock.SoundId = ReplaceAudGit("https://github.com/Potato032/Entt/blob/main/ClockSoundsCreditsToVynixus.mp3?raw=true","ClockSounds") 
clock.Volume = 7
clock:Play()
-- The Speed
game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 10
-- The Effect 1
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(71, 151, 211)
game.Lighting.MainColorCorrection.Contrast = 0
game.Lighting.MainColorCorrection.Saturation = 0.1
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(10), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(180),{TintColor = Color3.fromRGB(255, 255, 255)})
TW:Play()
-- The Effect 2
local idk = Instance.new("ScreenGui")
local ImageLabel = Instance.new("ImageLabel")
idk.IgnoreGuiInset = true
idk.Name = "idk"
idk.Parent = game:GetService("Players").LocalPlayer.PlayerGui
idk.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageLabel.Parent = idk
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel.BackgroundTransparency = 1.000
ImageLabel.Size = UDim2.new(1, 0, 0.998717964, 0)
ImageLabel.Image = "rbxassetid://190596490"
ImageLabel.ImageColor3 = Color3.fromRGB(0, 0, 0)

game.TweenService:Create(ImageLabel,TweenInfo.new(1),{ImageTransparency = 1}):Play()
ImageLabel.ImageTransparency = 1

game.TweenService:Create(ImageLabel,TweenInfo.new(7),{ImageTransparency = -10}):Play()
			wait(0.5)	
game.TweenService:Create(ImageLabel,TweenInfo.new(30),{ImageTransparency = -100}):Play()
-- Ended thing
      wait(31.5)
      chest:Destroy()
      saul:Destroy()
      wait(0.05)
loadstring(game:HttpGet("https://pastebin.com/raw/nJ78t5mw"))()
game.TweenService:Create(ImageLabel,TweenInfo.new(3),{ImageTransparency = 1}):Play()

